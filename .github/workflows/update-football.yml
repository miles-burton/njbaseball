name: Update Gridiron Index

on:
  push:
    branches: [main]
    paths:
      - football_scraper.py
      - .github/workflows/update-football.yml
  schedule:
    # Refresh schedules, scores, rankings, and all seven player stat groups.
    - cron: '45 5,11,17,23 * * *'
  workflow_dispatch:
    inputs:
      season:
        description: Optional NJ.com season override, like 2026-2027
        required: false
        default: ''

permissions:
  contents: write

concurrency:
  group: stats-data-updates
  cancel-in-progress: false

jobs:
  scrape-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install certifi

      - name: Refresh Gridiron Index data
        env:
          SEASON_OVERRIDE: ${{ github.event_name == 'workflow_dispatch' && inputs.season || '' }}
        run: |
          if [ -n "$SEASON_OVERRIDE" ]; then
            export FOOTBALL_SEASON="$SEASON_OVERRIDE"
          fi
          python3 football_scraper.py

      - name: Commit and push if data changed
        run: |
          git config user.name "Gridiron Index Bot"
          git config user.email "bot@gridironindex.com"
          git add js/football-data.js
          git diff --staged --quiet || git commit -m "Auto-update football stats $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
